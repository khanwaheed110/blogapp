spring:
  application:
    name: blogapp
  datasource:
    url: jdbc:mysql://localhost:3306/blogapp
    username: root
    password: Root@123
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
server:
  port: 8080
  